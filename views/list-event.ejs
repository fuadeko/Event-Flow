<!DOCTYPE html>
<html>

<head>
    <title>Manage Events</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">🎟️ Event Flow</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                    <% if (loggedIn) { %>
                        <li class="nav-item"><a class="nav-link" href="/admin/add_event">Add Event</a></li>
                        <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1 class="mb-4">Manage Events</h1>
        <div class="list-group">
            <% events.forEach(event=> { %>
                <div class="list-group-item">
                    <div class="d-flex justify-content-between align-items-center">
                        <h5>
                            <%= event.name %>
                        </h5>
                        <div>
                            <a href="/admin/<%= event.id %>/edit" class="btn btn-sm btn-warning me-2">Edit</a>
                            <a href="/admin/<%= event.id %>/delete" class="btn btn-sm btn-danger">Delete</a>
                        </div>
                    </div>
                    <p class="mb-0">
                        <%= event.location %> - <%= event.date.toDateString() %>
                    </p>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>